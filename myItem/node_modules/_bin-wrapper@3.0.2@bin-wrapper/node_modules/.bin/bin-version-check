#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_bin-version-check@2.1.0@bin-version-check/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_bin-version-check@2.1.0@bin-version-check/cli.js" "$@"
  ret=$?
fi
exit $ret
