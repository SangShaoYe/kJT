#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_rc@1.2.1@rc/index.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_rc@1.2.1@rc/index.js" "$@"
  ret=$?
fi
exit $ret
